#include <cstdio>
#define DEBUG 0

using namespace std;

int main()
{
  double s,money;
  while(scanf("%lf",&s) != EOF && s != 0){
    if(s <= 2){
      money = 10;
    }else{
      money = 10 + (s-2)*2.13;
    }
    printf("%.2f\n",money);
  }
  return 0;
}
